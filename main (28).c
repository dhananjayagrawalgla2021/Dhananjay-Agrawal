#include <stdio.h>
#include <math.h>
int main()
{
    float a,b;
    printf("Enter 1 Side of Equilateral triangle");
    scanf("%f", &a);
    b=a*a*0.433;
    printf("area is %f",b);

    return 0;
}
